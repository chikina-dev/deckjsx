interface:
  display_name: "deckjsx Slides"
  short_description: "Create clean-data-flow HTML-like JSX PPTX decks with deckjsx."
  default_prompt: "Use $deckjsx-slides to create a polished PPTX deck with deckjsx's current HTML-like JSX authoring surface and a clean component-graph/data-graph flow."
policy:
  allow_implicit_invocation: true
